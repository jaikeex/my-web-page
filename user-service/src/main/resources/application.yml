server:
  port: 8002
spring:
  jpa:
    database: mysql
  datasource:
    url: jdbc:mysql://localhost:3306/mwpdb
    username: root
    password: 1Thewinterstar197
  application:
    name: USER-SERVICE

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost